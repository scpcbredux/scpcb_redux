// use bevy::prelude::*;

// use crate::game::player::Player;

// use super::Scp106;

pub fn scp106_update(
    // nquery: Query<&Transform, (With<Scp106>, Without<Player>)>,
    // pquery: Query<&Transform, (With<Player>, Without<Scp106>)>,
) {
    // for ntransform in &nquery {
    //     for ptransform in &pquery {
    //         let dist = ntransform.translation.distance(ptransform.translation);
    //         if dist > 0.8 {
    //             if dist > 25.0 {
    //                 info!("Send player to pocket dimension at distance: {}", dist);
    //             } else {
    //                 info!("Move towards the player at distance: {}", dist);
    //             }
    //         }
    //     }
    // }
}
