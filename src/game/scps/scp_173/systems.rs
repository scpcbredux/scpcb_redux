use super::components::Scp173;
use bevy::prelude::*;

pub fn scp173_update(mut _query: Query<&mut Scp173>) {
    // if let Ok(mut scp_173) = query.get_single_mut() {
    //     if scp_173.idle != 3 {

    //     }
    // }
}
